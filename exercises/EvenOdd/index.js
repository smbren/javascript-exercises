/*
* is Even
*
* Given a number determine if a number is even
* without using a mathematic operator ( +, -, %, /, Math, ParseInt ecc. ecc. )
* nor a conditional operator
* 
* Examples:
* isEven(4) === true
* isEven(3) === false
*/

const isEven = n => {
    
};

module.exports = isEven;